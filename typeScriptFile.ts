// Example of fixing a TypeScript error by adding type annotations
function greet(name: string): string {
  return `Hello, ${name}!`;
}

// Example of fixing a TypeScript error by using the correct type
const names: string[] = ['Alice', 'Bob', 'Charlie'];
